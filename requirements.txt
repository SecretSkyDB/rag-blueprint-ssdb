# Inherited from NVIDIA-AI-Blueprints/rag v2.5.0 (kept in sync via UPSTREAM.md);
# only the toolkit + SSDB plug-in are added here.
#
# nvidia-nat~=1.0 is the renamed package (was `aiqtoolkit` pre-1.0). The CLI
# binary it ships is `nat`. Per
# https://docs.nvidia.com/nemo/agent-toolkit/latest/get-started/quick-start.html
nvidia-nat~=1.0

# The `nat-retriever-ssdb` plug-in is currently developed in the sibling repo
# at ../nat-retriever-ssdb/. Until its public v0.1.0 ships on PyPI at the
# design-partner kickoff, install it editable from there:
#
#   pip install -e ../nat-retriever-ssdb
#
# After v0.1.0 ships, replace the comment above with a real pin, e.g.:
#
#   nat-retriever-ssdb~=0.1
